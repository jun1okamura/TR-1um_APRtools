# 決定事項 1〜24

`apr/*.py` と `docs/` が根拠として引く **「決定 N」の出典**。
コード側の例: `apr/selfcheck.py`（決定 14）/ `apr/syn_equiv.py`（決定 14）/
`char/char_mem.py`（決定 23）/ `apr/config_base.py`（決定 17）。

★ **2026-09-17 まで、この表はどのリポジトリにも無かった**（U75）。
実体は作業ノート側にあり、**読めない出典を根拠として引いている**状態だった
（U10「参照先が移動していた」/ U52「履歴に一度も無いパスを読む」/
U66「名乗っている生成器が存在しない」と同じ形）。

★ **番号は変えない。** コードと docs が番号で引く（決定 20「表・台帳は 1 箇所」）。
足すときは 25 から。決定を**変えた**ときは、行を書き換えず**理由を添えて追記**する
（台帳と同じ作法。`docs/90_improvement_notes.md` §7 の読み方）。

| # | 項目 | 決定 |
|---|---|---|
| 1 | 参照方式 | **git submodule**（各設計に `tools/APRtools/`） |
| 2 | P&R スクリプトの正本 | **TR-1um_I2C_2026 の `scripts/pnr/` 世代** |
| 3 | 設定構造 | 薄皮を廃止 → `apr/config_base.py` + 各設計 `config.py` |
| 4 | STDCELL の正本 | **v59_4（行高 59.4）**。v64_8 は凍結 |
| 5 | コア幅 | **トラックピッチ 5.4 µm の整数倍**（`CORE_WIDTH_TRACKS`） |
| 6 | 電源ピン名 | コア側 **`vdd` / `vss`**、チップ側 `VDD` / `GND`。**写像は `rules` に 1 つだけ** |
| 7 | `*_nrow_fm` | **剥がす**。トップセル名は #18 の規則に合わせる |
| 8 | 環境変数 | **`APR_*`**。**名前は `config_base.ENV_KNOBS` に宣言する**（#20） |
| 9 | `legacy/` | **同梱**（read-only） |
| 10 | `pre_check.py` の `FRAME_CELL_NAMES` | **MPW テンプレート上流に PR**。こちらで値を変えない |
| 11 | DRC 値・レイヤ番号・フレームの実測値 | `apr/rules.py` に 1 本化。**写しを作らない**（`lint` の `drc-const`） |
| 12 | PDK 由来・設計固有 | **APRtools にコピーしない**（例外は `pdk/pending-upstream/`） |
| 13 | フレームの `OSS_DRV` | **TD4 / I2C_2026 版が正**。PDK へ PR push 済み・CI 通過 |
| 14 | **再現に効く値** | **設計の `config.py` に置く**。環境変数は掃引・実験用 |
| 15 | **設計で違う「床」** | チップ電源の方式・ロゴの置き場・段の順番も `config.py` |
| 16 | **個人のパス** | ドキュメントにも実行パスにも書かない。**生成物にも書かせない** |
| 17 | **既提出のものは壊さない** | 作り直すときは提出版を `reference/<世代>/` へ丸ごと移して README を付ける |
| 18 | **トップセル名** | **`tr_1um_<GitHub 名>_<設計の識別子>`**。`selfcheck` が形と `info.yaml` との一致を見る |
| 19 | **古い世代のデータを作業ディレクトリに残さない** | 出しっぱなしの NG が**本物の不一致を埋もれさせる** |
| 20 | **表・台帳は 1 箇所** | レール名の写像・掃引つまみ・フレームの寸法・`apr/` のファイル一覧。2 箇所に書くと片方に足し忘れる |
| 21 | **生産者と消費者は同じ場所を見る** | 書いた先を誰も読まない道具が **4 つ**あった（`mklvsnet` / `mkmemport` U28 / フレーム LVS ソース U40 / `gate_count` U52）。**既定の出力先は消費者が読む場所**から導く |
| 22 | **設計固有の値を共通側に直書きしない** | **3 件出た**（U36 `syn.sh` が I2C / U25 TB が TD4 / U26 `syn_equiv` が TD4）。**他の設計で引数なしに回すと黙って別設計の値を読む**。共通側は `config.py` から取り、無ければ止まる |
| 23 | **任意の依存を、必須の判定より前に置かない** | `check_chip_sim` が `matplotlib` 不在で判定ごと落ち、**全項目 PASS なのに終了コード 1** になった |
| 24 | **集合を `for` で回さない** | 反復順は `PYTHONHASHSEED` で変わるので、そこから**書き出す順**や**図形を差し込む順**を作ると同じ入力から違うファイルが出る（U1）。`sorted()` で包む。順序に意味が無いと分かっているなら `# lint: ok 理由`。`apr/lint.py` の `set-iter` が見る |

## そのあとの経過（決定そのものは変えていない）

- **決定 17** — 2026-09-17 に `MUXDFFRB` を直して I2C / SCLK_SPI を作り直したとき、
  **退避はせずに md5 を更新した**（設計者判断）。当時の提出物を再現する道は
  `TR-1um_I2C_2026/scripts/pnr/`（`lef/` の凍結コピーを読む）に残っている（U78）。
- **決定 21** — データ側でも同じ形が出た。セルの GDS を直しても、そこから作る
  `TR-1um_PNR.gds` を作り直さないと**古いセルでフローが回る**（U77）。
  `selfcheck` §3b が正本 2 つを毎回突き合わせる。
- **決定 11** — 名前が違うだけの写しは `lint --constants` で見えるようにした（U62 / U63）。
  残り 14 箇所は「値が同じだけの別物」なので**減らさないのが正しい**。
